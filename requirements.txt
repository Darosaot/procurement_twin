# Procurement Digital Twin — Python dependencies
# Versions pinned for reproducibility (prevents pickle deserialization
# errors if a dependency is silently upgraded in a deployment environment).

# ── Core data ──────────────────────────────────────────────────────
polars==1.26.0
pyarrow==19.0.0
pandas==2.2.3
numpy==2.2.0

# ── Machine learning ───────────────────────────────────────────────
scikit-learn==1.6.1
xgboost==2.1.4
statsmodels==0.14.4
shap==0.47.2

# ── Dashboard ──────────────────────────────────────────────────────
plotly==5.24.1
dash==2.18.2

# ── HF Hub (artifact download at container startup) ────────────────
huggingface_hub==0.27.1

# ── API server ─────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2

# ── API production features ────────────────────────────────────────
slowapi==0.1.9                  # Rate limiting for FastAPI/Starlette
prometheus-client==0.21.0      # Prometheus metrics exposition

# ── Local / dev only (not needed in the Space) ────────────────────
# notebook, matplotlib, seaborn  →  exploratory notebooks only
